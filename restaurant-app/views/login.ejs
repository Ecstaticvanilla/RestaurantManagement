<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Comic+Neue:wght@300;400;700&family=Playfair+Display:wght@400&display=swap" rel="stylesheet">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Alfredo's Restaurant</title>
    <style>
        /* Reset margin and padding for a clean slate */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body styling */
        body {
            font-family: 'Playfair Display', sans-serif;
            background-image: url('bgimg.jpg'); /* Background image */
            background-size: cover;
            background-position: center;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            overflow: hidden;
        }

        /* Dark overlay for background image */
        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
        }

        /* Header styling */
        header {
            position: absolute;
            top: 20px;
            left: 50%;
            transform: translateX(-50%);
            text-align: center;
            z-index: 10;
        }

        header h1 {
            color: papayawhip;
            font-size: 3rem;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
            letter-spacing: 1px;
            font-weight: 700;
            transition: transform 0.3s ease, letter-spacing 0.3s ease;
        }

        header h1:hover {
            letter-spacing: 2px;
            transform: scale(1.2);
        }

        /* Main content styling */
        .content {
            position: relative;
            z-index: 20;
            width: 100%;
            max-width: 400px;
            padding: 40px;
            background-color: rgba(0, 0, 0, 0.7); /* Darkened background for form */
            border-radius: 12px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.4);
        }

        .login-form h2 {
            text-align: center;
            color: #f1f1f1;
            margin-bottom: 20px;
            font-size: 2rem;
        }

        /* Input field styling */
        .login-form input {
            width: 100%;
            padding: 15px;
            margin: 10px 0;
            border: none;
            border-radius: 10px;
            background-color: rgba(255, 255, 255, 0.9);
            font-size: 1.1rem;
            color: #333;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .login-form input:focus {
            border-color: #ff7f00;
            box-shadow: 0 0 10px rgba(255, 127, 0, 0.7);
        }

        /* Button styling */
        .login-form button {
            width: 100%;
            padding: 15px;
            background-color: #ff7f00;
            color: white;
            font-size: 1.2rem;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .login-form button:hover {
            background-color: #f57c00;
            transform: translateY(-3px);
        }

        /* Links styling */
        .login-form a {
            color: #ff7f00;
            text-decoration: none;
            display: block;
            text-align: center;
            margin-top: 15px;
            font-size: 1rem;
            transition: text-decoration 0.2s ease;
        }

        .login-form a:hover {
            text-decoration: underline;
        }

        /* Footer styling */
        footer {
            position: fixed;
            bottom: 0;
            width: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            text-align: center;
            color: white;
            font-size: 1rem;
            padding: 10px;
        }

        /* Responsive design */
        @media (max-width: 600px) {
            header h1 {
                font-size: 2.5rem;
            }

            .content {
                padding: 20px;
                width: 90%;
            }

            .login-form h2 {
                font-size: 1.8rem;
            }

            .login-form input {
                padding: 12px;
            }

            .login-form button {
                padding: 12px;
                font-size: 1.1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Background overlay -->
    <div class="overlay"></div>

    <!-- Header -->
    <header>
        <h1>Alfredo's Restaurant</h1>
    </header>

    <!-- Main Content -->
    <div class="content">
        <div class="login-form">
            <h2>Login</h2>
            <form action="/login" method="POST">
                <input type="text" id="username" name="username" placeholder="Username" required>
                <input type="password" id="password" name="password" placeholder="Password" required>
                <button type="submit">Login</button>
            </form>
            <a href="http://localhost:5500/restaurant-app/views/Welcome.html">Back</a>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 DBMS PROJECT SIDDH SARTHI SWAYAM.</p>
    </footer>
</body>
</html>
