<!-- In customers.ejs -->
<a href="http://localhost:5500/views/mainpage.html" target="_blank">HOME PAGE </a>

<table>
    <thead>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Phone Number</th>
            <th>Actions</th>
        </tr>
    </thead>
    <a href="/customers/new">Add New Customer</a>
    <tbody>
        <% customers.forEach(customer => { %>
            <tr>
                <td><%= customer.first_name %></td>
                <td><%= customer.last_name %></td>
                <td><%= customer.email %></td>
                <td><%= customer.phone_number %></td>
                <td>
                    <a href="/customers/edit/<%= customer.customer_id %>">Edit</a>
                    <form action="/customers/delete/<%= customer.customer_id %>" method="POST" style="display:inline;">
                        <button type="submit">Delete</button>
                    </form>
                    <a href="/customers/orders/<%= customer.customer_id %>">View Orders</a>
                    <form action="/customers/assign/<%= customer.customer_id %>" method="POST" style="display:inline;">
                        <input type="text" name="orderId" placeholder="Order ID" required>
                        <button type="submit">Assign</button>
                    </form>
                </td>
            </tr>
        <% }) %>
    </tbody>
</table>
